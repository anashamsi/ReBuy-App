import 'package:flutter/material.dart';

class LikeitemView extends StatelessWidget {
  const LikeitemView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Like Item Page')));
  }
}
